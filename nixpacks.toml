[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm install -g pnpm@11.19.0", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @devio/database generate && pnpm --filter @devio/api build"]

[start]
cmd = "pnpm --filter @devio/api start"
